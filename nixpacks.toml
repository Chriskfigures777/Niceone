[phases.setup]
nixPkgs = ["python311", "curl"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "export PATH=\"$HOME/.local/bin:$PATH\" && cd app && uv sync"
]

[start]
cmd = "cd app && export PATH=\"$HOME/.local/bin:$PATH\" && uv run python agent.py start"

