[phases.setup]
nixPkgs = ["python311", "curl"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "export PATH=\"$HOME/.local/bin:$PATH\"",
  "uv sync"
]

[start]
cmd = "export PATH=\"$HOME/.local/bin:$PATH\" && uv run python agent.py start"

